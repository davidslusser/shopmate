on: [push]

jobs:
  radon:
    uses: davidslusser/actions/.github/workflows/radon.yaml@main
    with:
      src: "django_project"
      options: ""
      pip_install_command: "pip install -r requirements.txt"
